#!/usr/bin/env bash

npm run unit

if [ -z "$SAUCE_USERNAME" ] && [ -z "$SAUCE_ACCESS_KEY"]; then
  echo
  echo "ATTENTION: Skipping E2E tests because the environment variables are not set!"
else
  npm run e2e:full
fi
